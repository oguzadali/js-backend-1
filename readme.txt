
**config.env oluşturuldu  
**npm istall dotenv --config.env dosyasının kullanımı için indirildi
**mongoose library added for mongo db


**npm install express-async-handler --save  indirildi
--hataları yakalamak için kütüphane

**npm install bcryptjs   indirildi
--alınan şifreleri kriptolayıp kaydetmek için kullanıldı
-- çalışmazsa password alırken arrow fonksiyona çevir

**npm install jsonwebtoken
--kullanıcı bilgilerini (token) şifreleyerek tarayıcı
belleğinde tutmak için kullandık  (bknz. jwt.io)

**npm installl multer  downloaded
--for upload images to backend
